[project]
name = "pedersen-accelerate"
authors = ["Pedersen Labs"]
description = "Collaborative financial management and tracking smart contract"
telemetry = false
requirements = []
[contracts.balance-sync]
path = "contracts/balance-sync.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
